// const productImageContainer = document.querySelector('.product-image-container');
// const productImage = productImageContainer.querySelector('.product-image');
// const zoomedImageContainer = productImageContainer.querySelector('.zoomed-image-container');
// const zoomedImage = zoomedImageContainer.querySelector('.zoomed-image');

// productImage.addEventListener('mousemove', (event) => {
//   const x = event.offsetX / productImage.offsetWidth;
//   const y = event.offsetY / productImage.offsetHeight;
//   const zoomX = -x * (zoomedImage.offsetWidth - zoomedImageContainer.offsetWidth);
//   const zoomY = -y * (zoomedImage.offsetHeight - zoomedImageContainer.offsetHeight);
//   zoomedImage.style.transform = `translate(${zoomX}px, ${zoomY}px)`;
// });


